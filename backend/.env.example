# ─────────────────────────────────────────────────────────────────
#  FleetFlow Backend — Environment Variables
#  Copy this file to .env and fill in your values.
#
#  Quick start with Docker:
#    docker compose up -d           # Start PostgreSQL
#    cp .env.example .env           # Copy this file
#    npm run prisma:migrate          # Run migrations
#    npm run prisma:seed             # Seed SuperAdmin + sample data
#    npm run dev                     # Start dev server (port 3001)
# ─────────────────────────────────────────────────────────────────

# ── Server ────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3001

# ── PostgreSQL — Prisma Connection URL ───────────────────────────
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Matches docker-compose.yml defaults:
DATABASE_URL="postgresql://fleetflow:fleetflow_dev@localhost:5432/fleetflow?schema=public"

# ── JWT ───────────────────────────────────────────────────────────
# Use a random 64-char string in production
JWT_SECRET="change-this-to-a-random-64-character-string-in-production!!!!!"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_SECRET="change-this-refresh-secret-too-in-production-please!!!!!"
JWT_REFRESH_EXPIRES_IN="30d"

# ── CORS — comma-separated allowed origins ────────────────────────
CORS_ORIGINS="http://localhost:5173,http://localhost:3000"

# ── Bcrypt ────────────────────────────────────────────────────────
BCRYPT_SALT_ROUNDS=12

# ── Rate Limiting ─────────────────────────────────────────────────
# 900000ms = 15min window | 100 requests per window per IP
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
